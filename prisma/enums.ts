/**
 * TypeScript enums matching Prisma schema definitions
 * For use throughout the application
 */

export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN'
}

export enum ContentType {
  TEXT = 'TEXT',
  IMAGE = 'IMAGE',
  MUSIC = 'MUSIC',
  VIDEO = 'VIDEO'
}
